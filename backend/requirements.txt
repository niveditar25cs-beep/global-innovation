# =============================================================================
# requirements.txt — Python dependencies for the backend application
#
# Versions are pinned for reproducible Docker image builds.
# Update by running: pip install --upgrade <package> && pip freeze > requirements.txt
# =============================================================================

# ─── Web framework
fastapi==0.139.2
uvicorn==0.51.0
python-multipart==0.0.32

# ─── Data validation
pydantic==2.12.5
pydantic-settings==2.14.2

# ─── Database (async + sync drivers)
sqlalchemy==2.0.51
asyncpg==0.31.0
psycopg2-binary==2.9.13
alembic==1.20.0

# ─── Cache / cursor store
redis==8.1.0

# ─── Data processing
pandas==2.3.3

# ─── HTTP client (used in tests and utilities)
requests==2.32.5

# ─── Configuration
python-dotenv==1.2.1
