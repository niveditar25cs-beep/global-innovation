# Server Configuration
FLASK_APP=app.py
FLASK_ENV=development
PORT=5000
DEBUG=True

# Model Directories (Optional: defaults to backend/ml/model)
# MODEL_DIR=./ml/model

# CORS allowed origins (comma-separated or * for all)
CORS_ORIGINS=*
